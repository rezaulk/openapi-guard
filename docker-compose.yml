services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: openapiguard
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: OpenApiGuardDb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
